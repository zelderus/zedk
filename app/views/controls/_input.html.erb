
<%# 
	name - имя
	type - тип инпута
	clss - имя классса стиля

	Примеры:
			= render "controls/input", type: 'hidden', name: 'ub'

 %>

 <%
	if (!defined? type) then type = 'text' end
	if (!defined? clss) then clss = '' end
 %>


<input class='ctrl-inp ctrl-inp-norm <%= clss %>' type='<%= type %>' name='<%= name %>'/>
